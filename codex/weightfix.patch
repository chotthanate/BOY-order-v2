--- index.html
+++ index.html
@@ -180,6 +180,24 @@
     window.safeLocalStorageSet = function(key, value) {
         try { localStorage.setItem(key, value); } catch(e) { console.log("Local draft skipped"); }
     }
+    window.cleanItemName = function(item) {
+        return String(item || '').trim();
+    }
+    window.uniqueItemList = function(items) {
+        return Array.from(new Set((items || []).map(window.cleanItemName).filter(Boolean)));
+    }
+    window.getAllKnownItems = function() {
+        let known = [];
+        Object.keys(window.activeCategories || {}).forEach(cat => {
+            known = known.concat(window.activeCategories[cat] || []);
+        });
+        return window.uniqueItemList(known);
+    }
+    window.normalizeWeightItems = function() {
+        const knownItems = new Set(window.getAllKnownItems());
+        window.weightItems = window.uniqueItemList(window.weightItems).filter(item => knownItems.has(item));
+        return window.weightItems;
+    }
     window.getBusinessDateKey = function(date = new Date()) {
         try {
             const parts = new Intl.DateTimeFormat('en-CA', {
@@ -211,6 +229,7 @@
     window.returnQty = {}; 
     window.returnWgt = {};
     window.revenue = { cash: "", transfer: "" };
+    window.databaseSaveInProgress = false;
 </script>
 
 <script type="module">
@@ -228,9 +247,12 @@
     // โหลดการเปลี่ยนแปลงเมนูแบบเงียบๆ เบื้องหลัง
     onSnapshot(doc(db, "system_settings", "master_database"), (docSnap) => {
         if (docSnap.exists()) {
+            if (window.databaseSaveInProgress && !docSnap.metadata.hasPendingWrites) return;
+
             window.activeCategories = docSnap.data().categories || {};
             window.weightItems = docSnap.data().weightItems || [];
             window.itemMappings = docSnap.data().itemMappings || {};
+            window.normalizeWeightItems();
 
             window.safeLocalStorageSet('boy_v4_database', JSON.stringify(window.activeCategories));
             window.safeLocalStorageSet('boy_v4_weights', JSON.stringify(window.weightItems));
@@ -316,6 +338,7 @@
     // 🚀 ระบบเปิดแอปครั้งแรก
     // ==========================================
     function initApp() {
+        window.normalizeWeightItems();
         const dateKey = window.getBusinessDateKey();
         const localBackup = window.safeLocalStorageGet('boy_draft_' + dateKey);
         let localDraftTimestamp = 0;
@@ -563,17 +586,27 @@
     // ==========================================
     // 🚀 ระบบจัดการฐานข้อมูล (เขียนกลับเข้า Firebase)
     // ==========================================
-    window.saveDB = function() {
-        if (window.db) {
-            window.setDoc(window.doc(window.db, "system_settings", "master_database"), {
+    window.saveDB = async function() {
+        if (!window.db || !window.setDoc || !window.doc) throw new Error("ฐานข้อมูลยังไม่พร้อม กรุณาลองใหม่อีกครั้ง");
+
+        window.normalizeWeightItems();
+        window.safeLocalStorageSet('boy_v4_database', JSON.stringify(window.activeCategories));
+        window.safeLocalStorageSet('boy_v4_weights', JSON.stringify(window.weightItems));
+        window.safeLocalStorageSet('boy_v4_mappings', JSON.stringify(window.itemMappings));
+
+        window.databaseSaveInProgress = true;
+        try {
+            await window.setDoc(window.doc(window.db, "system_settings", "master_database"), {
                 categories: window.activeCategories,
                 weightItems: window.weightItems,
                 itemMappings: window.itemMappings
             }, { merge: true });
+        } finally {
+            window.databaseSaveInProgress = false;
         }
     }
     
-    window.saveItemData = function() {
+    window.saveItemData = async function() {
         const cat = document.getElementById('item-cat').value;
         const name = document.getElementById('item-name').value.trim();
         const unit = document.getElementById('item-unit').value.trim();
@@ -582,29 +615,40 @@
         
         if(!name) return;
         const fullName = unit ? `${name} (${unit})` : name;
-        
-        if (window.editingItem) {
-            if (window.editingItem.cat === cat) {
-                let idx = window.activeCategories[cat].indexOf(window.editingItem.oldName);
-                if (idx !== -1) window.activeCategories[cat][idx] = fullName;
+
+        try {
+            const oldName = window.editingItem ? window.editingItem.oldName : null;
+
+            if (window.editingItem) {
+                if (window.editingItem.cat === cat) {
+                    let idx = window.activeCategories[cat].indexOf(oldName);
+                    if (idx !== -1) window.activeCategories[cat][idx] = fullName;
+                } else {
+                    window.activeCategories[window.editingItem.cat] = window.activeCategories[window.editingItem.cat].filter(i => i !== oldName);
+                    if(!window.activeCategories[cat]) window.activeCategories[cat] = [];
+                    window.activeCategories[cat].push(fullName);
+                }
+                if (oldName !== fullName) delete window.itemMappings[oldName];
             } else {
-                window.activeCategories[window.editingItem.cat] = window.activeCategories[window.editingItem.cat].filter(i => i !== window.editingItem.oldName);
                 if(!window.activeCategories[cat]) window.activeCategories[cat] = [];
                 window.activeCategories[cat].push(fullName);
             }
-            window.weightItems = window.weightItems.filter(i => i !== window.editingItem.oldName);
-            if (window.editingItem.oldName !== fullName) delete window.itemMappings[window.editingItem.oldName];
-        } else {
-            if(!window.activeCategories[cat]) window.activeCategories[cat] = [];
-            window.activeCategories[cat].push(fullName);
+
+            window.weightItems = window.uniqueItemList(window.weightItems).filter(i => i !== oldName && i !== fullName);
+            if (isWeight) window.weightItems.push(fullName);
+            window.weightItems = window.uniqueItemList(window.weightItems);
+
+            if (sheetName) window.itemMappings[fullName] = sheetName; else delete window.itemMappings[fullName];
+
+            await window.saveDB();
+            window.cancelEditMode();
+            window.renderManageList();
+            window.renderOrder();
+            window.renderReceive();
+            window.renderReturn();
+        } catch(e) {
+            alert("❌ บันทึกฐานข้อมูลไม่สำเร็จ: " + e.message);
         }
-        
-        if (isWeight) window.weightItems.push(fullName);
-        if (sheetName) window.itemMappings[fullName] = sheetName; else delete window.itemMappings[fullName];
-        
-        window.saveDB();
-        window.cancelEditMode();
-        window.renderManageList();
     }
 
     // ฟังก์ชันย่อยอื่นๆ (UI/UX)
@@ -620,9 +664,9 @@
     window.openManageModal = function() { document.getElementById('manage-modal').style.display = 'flex'; window.renderManageList(); }
     window.closeManageModal = function() { document.getElementById('manage-modal').style.display = 'none'; }
     window.renderManageList = function() { const search = document.getElementById('manage-search').value.toLowerCase(); const tbody = document.getElementById('manage-table-body'); tbody.innerHTML = ''; const nav = document.getElementById('manage-category-nav'); nav.innerHTML = ''; window.categoryOrder.forEach((cat, ci) => { if(!window.activeCategories[cat] || window.activeCategories[cat].length === 0) return; const b = document.createElement('button'); b.className = 'cat-nav-btn'; b.innerText = cat.split(' ')[1] || cat; b.onclick = () => document.getElementById(`cat-target-${ci}`).scrollIntoView({behavior:'smooth'}); nav.appendChild(b); let rows = ''; window.activeCategories[cat].forEach((item, idx) => { if (!search || item.toLowerCase().includes(search)) { const safeItem = item.replace(/'/g, "\\'"); const safeCat = cat.replace(/'/g, "\\'"); rows += `<tr><td><b>${item}</b>${window.itemMappings[item]?`<br><small style="color:orange;">➡️ ${window.itemMappings[item]}</small>`:''}</td><td style="text-align:center;">${window.weightItems.includes(item)?'⚖️':''}</td><td><div style="display:flex; gap:4px; justify-content:center;"><button class="btn-icon" onclick="window.moveItem('${safeCat}',${idx},-1)">⬆️</button><button class="btn-icon" onclick="window.moveItem('${safeCat}',${idx},1)">⬇️</button><button class="btn-icon" style="color:blue" onclick="window.editItemBtn('${safeCat}','${safeItem}')">✏️</button><button class="btn-icon" style="color:red" onclick="window.deleteItemBtn('${safeCat}','${safeItem}')">🗑️</button></div></td></tr>`; } }); if(rows) { tbody.innerHTML += `<tr id="cat-target-${ci}" class="cat-header-row"><td colspan="3">${cat}</td></tr>` + rows; } }); }
-    window.moveItem = function(cat, idx, dir) { let items = window.activeCategories[cat]; let target = idx + dir; if (target >= 0 && target < items.length) { let temp = items[idx]; items[idx] = items[target]; items[target] = temp; window.renderManageList(); window.saveDB(); } }
+    window.moveItem = async function(cat, idx, dir) { let items = window.activeCategories[cat]; let target = idx + dir; if (target >= 0 && target < items.length) { let temp = items[idx]; items[idx] = items[target]; items[target] = temp; window.renderManageList(); try { await window.saveDB(); } catch(e) { alert("❌ บันทึกลำดับไม่สำเร็จ: " + e.message); } } }
     window.editItemBtn = function(cat, oldFullName) { window.editingItem = { cat: cat, oldName: oldFullName }; document.getElementById('manage-form-title').innerText = "✏️ แก้ไขสินค้า"; document.getElementById('item-cat').value = cat; let name = oldFullName; let unit = ""; let match = oldFullName.match(/(.*?)\s*\((.*?)\)$/); if (match) { name = match[1].trim(); unit = match[2].trim(); } document.getElementById('item-name').value = name; document.getElementById('item-unit').value = unit; document.getElementById('item-sheet-name').value = window.itemMappings[oldFullName] || ""; document.getElementById('item-is-weight').checked = window.weightItems.includes(oldFullName); document.getElementById('btn-cancel-edit').style.display = 'block'; document.getElementById('manage-scroll-area').scrollTop = 0; }
-    window.deleteItemBtn = function(cat, item) { if(confirm(`ลบ "${item}"?`)) { window.activeCategories[cat] = window.activeCategories[cat].filter(i => i !== item); delete window.itemMappings[item]; window.renderManageList(); window.saveDB(); } }
+    window.deleteItemBtn = async function(cat, item) { if(confirm(`ลบ "${item}"?`)) { window.activeCategories[cat] = window.activeCategories[cat].filter(i => i !== item); window.weightItems = window.weightItems.filter(i => i !== item); delete window.itemMappings[item]; delete window.receiveQty[item]; delete window.receiveWgt[item]; delete window.returnQty[item]; delete window.returnWgt[item]; window.renderManageList(); try { await window.saveDB(); window.renderReceive(); window.renderReturn(); } catch(e) { alert("❌ ลบสินค้าไม่สำเร็จ: " + e.message); } } }
     window.cancelEditMode = function() { window.editingItem = null; document.getElementById('manage-form-title').innerText = "➕ เพิ่มสินค้าใหม่"; document.getElementById('item-name').value = ''; document.getElementById('item-unit').value = ''; document.getElementById('item-sheet-name').value = ''; document.getElementById('item-is-weight').checked = false; document.getElementById('btn-cancel-edit').style.display = 'none'; }
 
     let startX = 0; window.swipeStart = function(e) { startX = e.touches[0].clientX; e.currentTarget.style.transition = 'none'; }
