--- index.html
+++ index.html
@@ -198,6 +198,34 @@
         window.weightItems = window.uniqueItemList(window.weightItems).filter(item => knownItems.has(item));
         return window.weightItems;
     }
+    window.getDatabaseTimestamp = function(database) {
+        const time = Date.parse(database?.updatedAt || '');
+        return Number.isNaN(time) ? 0 : time;
+    }
+    window.persistMasterDatabase = function(updatedAt) {
+        window.normalizeWeightItems();
+        const nextTimestamp = window.getDatabaseTimestamp({ updatedAt });
+        if (nextTimestamp > 0) window.databaseUpdatedAt = nextTimestamp;
+        window.safeLocalStorageSet('boy_v4_database', JSON.stringify(window.activeCategories));
+        window.safeLocalStorageSet('boy_v4_weights', JSON.stringify(window.weightItems));
+        window.safeLocalStorageSet('boy_v4_mappings', JSON.stringify(window.itemMappings));
+        window.safeLocalStorageSet('boy_v4_database_updated_at', String(window.databaseUpdatedAt || 0));
+    }
+    window.applyMasterDatabase = function(database, options = {}) {
+        const incoming = database || {};
+        const categories = incoming.categories || {};
+        if (Object.keys(categories).length === 0) return false;
+
+        const incomingTimestamp = window.getDatabaseTimestamp(incoming);
+        if (!options.force && window.databaseUpdatedAt > 0 && incomingTimestamp === 0) return false;
+        if (!options.force && incomingTimestamp > 0 && incomingTimestamp < window.databaseUpdatedAt) return false;
+
+        window.activeCategories = categories;
+        window.weightItems = incoming.weightItems || [];
+        window.itemMappings = incoming.itemMappings || {};
+        window.persistMasterDatabase(incoming.updatedAt || "");
+        return true;
+    }
     window.getBusinessDateKey = function(date = new Date()) {
         try {
             const parts = new Intl.DateTimeFormat('en-CA', {
@@ -229,34 +257,31 @@
     window.returnQty = {}; 
     window.returnWgt = {};
     window.revenue = { cash: "", transfer: "" };
-    window.databaseSaveInProgress = false;
+    window.databaseUpdatedAt = Number(window.safeLocalStorageGet('boy_v4_database_updated_at') || 0);
 </script>
 
 <script type="module">
     import { initializeApp } from "https://www.gstatic.com/firebasejs/10.10.0/firebase-app.js";
-    import { getFirestore, doc, setDoc, onSnapshot } from "https://www.gstatic.com/firebasejs/10.10.0/firebase-firestore.js";
+    import { getFirestore, doc, onSnapshot } from "https://www.gstatic.com/firebasejs/10.10.0/firebase-firestore.js";
     
     const firebaseConfig = { apiKey: "AIzaSyAch0s5J7zaWE--YgCRjtwzH__YYKRcfvw", authDomain: "bibc-acc.firebaseapp.com", projectId: "bibc-acc", storageBucket: "bibc-acc.firebasestorage.app", messagingSenderId: "476095491720", appId: "1:476095491720:web:3a915c7b903ea83870793d" };
     const app = initializeApp(firebaseConfig); 
     const db = getFirestore(app);
     
     window.db = db; 
-    window.setDoc = setDoc; 
     window.doc = doc;
     
     // โหลดการเปลี่ยนแปลงเมนูแบบเงียบๆ เบื้องหลัง
     onSnapshot(doc(db, "system_settings", "master_database"), (docSnap) => {
         if (docSnap.exists()) {
-            if (window.databaseSaveInProgress && !docSnap.metadata.hasPendingWrites) return;
-
-            window.activeCategories = docSnap.data().categories || {};
-            window.weightItems = docSnap.data().weightItems || [];
-            window.itemMappings = docSnap.data().itemMappings || {};
-            window.normalizeWeightItems();
-
-            window.safeLocalStorageSet('boy_v4_database', JSON.stringify(window.activeCategories));
-            window.safeLocalStorageSet('boy_v4_weights', JSON.stringify(window.weightItems));
-            window.safeLocalStorageSet('boy_v4_mappings', JSON.stringify(window.itemMappings));
+            const dbData = docSnap.data() || {};
+            const applied = window.applyMasterDatabase({
+                categories: dbData.categories || {},
+                weightItems: dbData.weightItems || [],
+                itemMappings: dbData.itemMappings || {},
+                updatedAt: dbData.updatedAt || ""
+            });
+            if (!applied) return;
 
             if(window.renderOrder) window.renderOrder();
             if(window.renderReceive) window.renderReceive();
@@ -366,6 +391,15 @@
         window.renderOrder();
         window.renderReceive();
         window.renderReturn();
+
+        apiCall('loadDB').then(res => {
+            if (window.applyMasterDatabase(res.database || {})) {
+                window.renderOrder();
+                window.renderReceive();
+                window.renderReturn();
+                if(document.getElementById('manage-modal').style.display === 'flex') window.renderManageList();
+            }
+        }).catch(e => console.log("Load database skipped"));
 
         // แอบเช็คฉบับร่างบน Cloud เผื่อเปลี่ยนมือถือ
         apiCall('loadDraft').then(res => {
@@ -584,26 +618,20 @@
     }
 
     // ==========================================
-    // 🚀 ระบบจัดการฐานข้อมูล (เขียนกลับเข้า Firebase)
+    // 🚀 ระบบจัดการฐานข้อมูล (เขียนผ่าน Apps Script แล้วอ่านกลับจาก Cloud)
     // ==========================================
     window.saveDB = async function() {
-        if (!window.db || !window.setDoc || !window.doc) throw new Error("ฐานข้อมูลยังไม่พร้อม กรุณาลองใหม่อีกครั้ง");
-
         window.normalizeWeightItems();
-        window.safeLocalStorageSet('boy_v4_database', JSON.stringify(window.activeCategories));
-        window.safeLocalStorageSet('boy_v4_weights', JSON.stringify(window.weightItems));
-        window.safeLocalStorageSet('boy_v4_mappings', JSON.stringify(window.itemMappings));
-
-        window.databaseSaveInProgress = true;
-        try {
-            await window.setDoc(window.doc(window.db, "system_settings", "master_database"), {
-                categories: window.activeCategories,
-                weightItems: window.weightItems,
-                itemMappings: window.itemMappings
-            }, { merge: true });
-        } finally {
-            window.databaseSaveInProgress = false;
-        }
+        const database = {
+            categories: window.activeCategories,
+            weightItems: window.weightItems,
+            itemMappings: window.itemMappings,
+            updatedAt: new Date().toISOString()
+        };
+
+        window.applyMasterDatabase(database, { force: true });
+        const res = await apiCall('saveDB', { database });
+        if (res.database) window.applyMasterDatabase(res.database, { force: true });
     }
     
     window.saveItemData = async function() {
