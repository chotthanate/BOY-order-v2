--- index.html
+++ index.html
@@ -170,10 +170,40 @@
     window.categoryOrder = ["🍉 ผลไม้ (ใส่กล่อง)", "🍍 ผลไม้ (โชว์)", "🥛 อื่นๆ", "🥫 ผลไม้อบแห้งกระปุก", "🍬 ขนม", "❄️ ผลไม้แช่แข็ง", "📦 บรรจุภัณฑ์", "🍪 ท็อปปิ้ง", "☕ ผง"];
     window.VIP_ITEMS = ["ส้ม (โหล)", "อโวคาโด (กล่อง)", "แตงโม (กล่อง)", "สับปะรด (สกัด+ปั่น) (กล่อง)"];
 
+    window.APP_TIME_ZONE = 'Asia/Bangkok';
+    window.safeJsonParse = function(value, fallback) {
+        try { return value ? JSON.parse(value) : fallback; } catch(e) { return fallback; }
+    }
+    window.safeLocalStorageGet = function(key) {
+        try { return localStorage.getItem(key); } catch(e) { return null; }
+    }
+    window.safeLocalStorageSet = function(key, value) {
+        try { localStorage.setItem(key, value); } catch(e) { console.log("Local draft skipped"); }
+    }
+    window.getBusinessDateKey = function(date = new Date()) {
+        try {
+            const parts = new Intl.DateTimeFormat('en-CA', {
+                timeZone: window.APP_TIME_ZONE,
+                year: 'numeric',
+                month: '2-digit',
+                day: '2-digit'
+            }).formatToParts(date).reduce((acc, part) => {
+                acc[part.type] = part.value;
+                return acc;
+            }, {});
+            return `${parts.year}-${parts.month}-${parts.day}`;
+        } catch(e) {
+            const y = date.getFullYear();
+            const m = String(date.getMonth() + 1).padStart(2, '0');
+            const d = String(date.getDate()).padStart(2, '0');
+            return `${y}-${m}-${d}`;
+        }
+    }
+
     // 🌟 ดึงข้อมูลจากความจำมือถือทันทีที่เปิดแอป (ทำให้หน้าจอไม่ว่างเปล่า)
-    window.activeCategories = JSON.parse(localStorage.getItem('boy_v4_database')) || {};
-    window.weightItems = JSON.parse(localStorage.getItem('boy_v4_weights')) || [];
-    window.itemMappings = JSON.parse(localStorage.getItem('boy_v4_mappings')) || {};
+    window.activeCategories = window.safeJsonParse(window.safeLocalStorageGet('boy_v4_database'), {});
+    window.weightItems = window.safeJsonParse(window.safeLocalStorageGet('boy_v4_weights'), []);
+    window.itemMappings = window.safeJsonParse(window.safeLocalStorageGet('boy_v4_mappings'), {});
 
     window.orderCart = {}; 
     window.receiveQty = {}; 
@@ -202,9 +232,9 @@
             window.weightItems = docSnap.data().weightItems || [];
             window.itemMappings = docSnap.data().itemMappings || {};
 
-            localStorage.setItem('boy_v4_database', JSON.stringify(window.activeCategories));
-            localStorage.setItem('boy_v4_weights', JSON.stringify(window.weightItems));
-            localStorage.setItem('boy_v4_mappings', JSON.stringify(window.itemMappings));
+            window.safeLocalStorageSet('boy_v4_database', JSON.stringify(window.activeCategories));
+            window.safeLocalStorageSet('boy_v4_weights', JSON.stringify(window.weightItems));
+            window.safeLocalStorageSet('boy_v4_mappings', JSON.stringify(window.itemMappings));
 
             if(window.renderOrder) window.renderOrder();
             if(window.renderReceive) window.renderReceive();
@@ -238,23 +268,42 @@
         }
     }
 
+    function getDraftTimestamp(draft) {
+        const time = Date.parse(draft?.timestamp || '');
+        return Number.isNaN(time) ? 0 : time;
+    }
+
+    function hydrateDraft(draft) {
+        window.orderCart = draft.cart || {};
+        window.receiveQty = draft.receiveQty || {};
+        window.receiveWgt = draft.receiveWgt || {};
+        window.returnQty = draft.returnQty || {};
+        window.returnWgt = draft.returnWgt || {};
+        window.revenue = draft.revenue || {cash:"", transfer:""};
+        window.VIP_ITEMS.forEach(i => { if (window.returnQty[i] === undefined) window.returnQty[i] = 0; });
+        document.getElementById('revenue-cash').value = window.revenue.cash || '';
+        document.getElementById('revenue-transfer').value = window.revenue.transfer || '';
+    }
+
     // ==========================================
     // 🚀 ระบบ AUTOSAVE แบบมือถือ (ไม่หายแน่นอน)
     // ==========================================
     let autoSaveTimer = null;
     function scheduleAutoSave() {
-        const dateKey = new Date().toLocaleDateString('en-CA');
+        const dateKey = window.getBusinessDateKey();
         const draft = { 
             cart: window.orderCart, 
             receiveQty: window.receiveQty, 
             receiveWgt: window.receiveWgt, 
             returnQty: window.returnQty, 
             returnWgt: window.returnWgt, 
-            revenue: window.revenue 
+            revenue: window.revenue,
+            timestamp: new Date().toISOString(),
+            date: dateKey
         };
         
         // ก๊อก 1: ฝังลงมือถือแบบ Real-time (0 วิ)
-        localStorage.setItem('boy_draft_' + dateKey, JSON.stringify(draft));
+        window.safeLocalStorageSet('boy_draft_' + dateKey, JSON.stringify(draft));
 
         // ก๊อก 2: ส่งขึ้น Google Sheet แบบใจเย็น
         clearTimeout(autoSaveTimer);
@@ -267,19 +316,16 @@
     // 🚀 ระบบเปิดแอปครั้งแรก
     // ==========================================
     function initApp() {
-        const dateKey = new Date().toLocaleDateString('en-CA');
-        const localBackup = localStorage.getItem('boy_draft_' + dateKey);
+        const dateKey = window.getBusinessDateKey();
+        const localBackup = window.safeLocalStorageGet('boy_draft_' + dateKey);
+        let localDraftTimestamp = 0;
 
         // งัดข้อมูลจากความจำมือถือขึ้นมาโชว์ทันที
         if (localBackup) {
             try {
                 const bData = JSON.parse(localBackup);
-                window.orderCart = bData.cart || {};
-                window.receiveQty = bData.receiveQty || {};
-                window.receiveWgt = bData.receiveWgt || {};
-                window.returnQty = bData.returnQty || {};
-                window.returnWgt = bData.returnWgt || {};
-                window.revenue = bData.revenue || {cash:"", transfer:""};
+                hydrateDraft(bData);
+                localDraftTimestamp = getDraftTimestamp(bData);
             } catch(e) {}
         }
 
@@ -300,18 +346,12 @@
 
         // แอบเช็คฉบับร่างบน Cloud เผื่อเปลี่ยนมือถือ
         apiCall('loadDraft').then(res => {
-            if (res.draft && !localBackup) {
-                window.orderCart = res.draft.cart || {};
-                window.receiveQty = res.draft.receiveQty || {};
-                window.receiveWgt = res.draft.receiveWgt || {};
-                window.returnQty = res.draft.returnQty || {};
-                window.returnWgt = res.draft.returnWgt || {};
-                window.revenue = res.draft.revenue || {cash:"", transfer:""};
-
-                document.getElementById('revenue-cash').value = window.revenue.cash || '';
-                document.getElementById('revenue-transfer').value = window.revenue.transfer || '';
-                window.VIP_ITEMS.forEach(i => { if (window.returnQty[i] === undefined) window.returnQty[i] = 0; });
-
+            const serverDraft = res.draft || {};
+            const hasServerDraft = Object.keys(serverDraft).length > 0;
+            const serverDraftTimestamp = getDraftTimestamp(serverDraft);
+            if (hasServerDraft && (!localBackup || serverDraftTimestamp > localDraftTimestamp)) {
+                hydrateDraft(serverDraft);
+                window.safeLocalStorageSet('boy_draft_' + dateKey, JSON.stringify(serverDraft));
                 window.renderOrder(); window.renderReceive(); window.renderReturn();
             }
         }).catch(e => console.log("Load draft skipped"));
@@ -587,10 +627,27 @@
 
     let startX = 0; window.swipeStart = function(e) { startX = e.touches[0].clientX; e.currentTarget.style.transition = 'none'; }
     window.swipeMove = function(e) { let dX = startX - e.touches[0].clientX; if (dX > 10) e.currentTarget.style.transform = `translateX(-${Math.min(dX, 100)}px)`; }
-    window.swipeEnd = function(e, item, type) { let dX = startX - e.changedTouches[0].clientX; e.currentTarget.style.transition = 'transform 0.3s ease'; if (dX > 70) { if(type==='receive') { delete window.receiveQty[item]; window.renderReceive(); } else { delete window.returnQty[item]; window.renderReturn(); } scheduleAutoSave(); } else { e.currentTarget.style.transform = `translateX(0)`; } }
-
-    window.showSuggestionsRec = function(v) { const l = document.getElementById('autocomplete-list-rec'); l.innerHTML = ''; if(!v) return; window.categoryOrder.forEach(c => { if(window.activeCategories[c]) window.activeCategories[c].filter(i => i.toLowerCase().includes(v.toLowerCase())).forEach(m => { const d = document.createElement('div'); d.innerText = m; d.onclick = () => { window.receiveQty[m] = 0; scheduleAutoSave(); window.renderReceive(); l.innerHTML = ''; document.getElementById('receive-search').value = ''; setTimeout(() => { const target = document.getElementById(`item-rec-${m.replace(/'/g, "\\'")}`); if(target) target.scrollIntoView({ behavior: 'smooth', block: 'center' }); }, 150); }; l.appendChild(d); }); }); }
-    window.showSuggestions = function(v) { const l = document.getElementById('autocomplete-list'); l.innerHTML = ''; if(!v) return; window.categoryOrder.forEach(c => { if(window.activeCategories[c]) window.activeCategories[c].filter(i => i.toLowerCase().includes(v.toLowerCase())).forEach(m => { const d = document.createElement('div'); d.innerText = m; d.onclick = () => { window.returnQty[m] = 0; scheduleAutoSave(); window.renderReturn(); l.innerHTML = ''; document.getElementById('return-search').value = ''; setTimeout(() => { const target = document.getElementById(`item-ret-${m.replace(/'/g, "\\'")}`); if(target) target.scrollIntoView({ behavior: 'smooth', block: 'center' }); }, 150); }; l.appendChild(d); }); }); }
+    window.swipeEnd = function(e, item, type) {
+        let dX = startX - e.changedTouches[0].clientX;
+        e.currentTarget.style.transition = 'transform 0.3s ease';
+        if (dX > 70) {
+            if(type==='receive') {
+                delete window.receiveQty[item];
+                delete window.receiveWgt[item];
+                window.renderReceive();
+            } else {
+                delete window.returnQty[item];
+                delete window.returnWgt[item];
+                window.renderReturn();
+            }
+            scheduleAutoSave();
+        } else {
+            e.currentTarget.style.transform = `translateX(0)`;
+        }
+    }
+
+    window.showSuggestionsRec = function(v) { const l = document.getElementById('autocomplete-list-rec'); l.innerHTML = ''; if(!v) return; window.categoryOrder.forEach(c => { if(window.activeCategories[c]) window.activeCategories[c].filter(i => i.toLowerCase().includes(v.toLowerCase())).forEach(m => { const d = document.createElement('div'); d.innerText = m; d.onclick = () => { if (window.receiveQty[m] === undefined) window.receiveQty[m] = 0; scheduleAutoSave(); window.renderReceive(); l.innerHTML = ''; document.getElementById('receive-search').value = ''; setTimeout(() => { const target = document.getElementById(`item-rec-${m.replace(/'/g, "\\'")}`); if(target) target.scrollIntoView({ behavior: 'smooth', block: 'center' }); }, 150); }; l.appendChild(d); }); }); }
+    window.showSuggestions = function(v) { const l = document.getElementById('autocomplete-list'); l.innerHTML = ''; if(!v) return; window.categoryOrder.forEach(c => { if(window.activeCategories[c]) window.activeCategories[c].filter(i => i.toLowerCase().includes(v.toLowerCase())).forEach(m => { const d = document.createElement('div'); d.innerText = m; d.onclick = () => { if (window.returnQty[m] === undefined) window.returnQty[m] = 0; scheduleAutoSave(); window.renderReturn(); l.innerHTML = ''; document.getElementById('return-search').value = ''; setTimeout(() => { const target = document.getElementById(`item-ret-${m.replace(/'/g, "\\'")}`); if(target) target.scrollIntoView({ behavior: 'smooth', block: 'center' }); }, 150); }; l.appendChild(d); }); }); }
 
     window.onload = initApp;
 </script>
